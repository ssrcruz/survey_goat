

<div id="layout" class="content pure-g">
    <div id="nav" class="pure-u">
      <a href="#" class="nav-menu-button">Menu</a>

        <div class="nav-inner">
          <button class="primary-button pure-button">
            <%= link_to 'New Survey', new_survey_path, remote: true %>
          </button>

          <div class="pure-menu pure-menu-open">
            <ul>
              <li class="userNavInfo">
                <% if current_user %>
                  <div class="welcome">Welcome <%= current_user.name %>.</div>
                  <div class="logout">
                    <%= button_to "Log out", sessions_log_out_path, :method => "get" %>
                  </div>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
    </div>



<div id="list" class="pure-u-1">
  <div class="survey-item pure-g">
      <div class="pure-u" id="survey_list"><% @surveys.each do |survey| %>
        <%= render partial: 'survey', locals:{ survey:survey } %>
        <% end %>
      </div>
  </div>
</div>



<div id="main" class="pure-u-1">
    <div class="survey-content">
