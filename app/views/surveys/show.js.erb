// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
// You can use CoffeeScript in this file: http://coffeescript.org/

// $(function () {
//
//


<% if flash[:new] %>
var newSurvey = $("<%= escape_javascript(render(partial:"survey", locals:{survey:@survey}))%>").hide();
$('#survey_list').prepend(newSurvey);
newSurvey.fadeIn();
<% end %>

// $('.survey-item.pure-g').on('click', function() {
//     var item = $(this);
//
//     if (item.hasClass('test')) {
//       li.removeClass('test');
//     }else {
//       li.addClass('test');
//     }

$('#main').html("<%= escape_javascript(render("show"))%>");
